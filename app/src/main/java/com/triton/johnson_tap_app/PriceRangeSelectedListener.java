package com.triton.johnson_tap_app;

public interface PriceRangeSelectedListener {
   // void priceRangeSelectedListener( String FA_BSD_BANKDT, String FA_BSD_AMOUNT);
   void priceRangeSelectedListener( int Count_value_start, int Count_value_end);
}
